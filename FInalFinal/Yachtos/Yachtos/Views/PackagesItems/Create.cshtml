@model Yachtos.Models.PackagesItems

@{
    ViewData["Title"] = "Create";
}

<h2>Create</h2>

<h4>PackagesItems</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">

            @foreach (var item in Model.getItemTypes())
            {
            <div>
                <label>@item</label>
                <select asp-for="ItemsId" asp-items="@(new SelectList(Model.getStorage(@item),"id","Item"))">

                    <option>--SelectOne---</option>
                </select>
            </div>
            }

            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
